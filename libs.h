// Bibliotecas

#include <stdio.h>
#include <stdlib.h>

// Estruturas de dados

struct shop {
    int maxClients;
    int spawnedClients;
    int openingTime;
    int closingTime;
    int timeCounter;
    int isItOpen;
    int durationOpen;
};
